import 'package:flutter/cupertino.dart';

class Deliveries extends StatelessWidget{
  const Deliveries({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Deliveries');
  }
}